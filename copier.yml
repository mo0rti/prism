_subdirectory: template
_templates_suffix: .jinja
_jinja_extensions:
  - jinja2_time.TimeExtension

_skip_if_exists:
  - .env
  - mobile-android/local.config.properties
  - mobile-ios/Config/Debug.xcconfig
  - mobile-ios/Config/Release.xcconfig

_exclude:
  - "{% if 'backend' not in platforms %}.env{% endif %}"
  - "{% if 'backend' not in platforms %}.env.example{% endif %}"
  - "{% if 'mobile-android' not in platforms %}mobile-android{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}mobile-ios{% endif %}"
  - "{% if 'backend' not in platforms %}backend{% endif %}"
  - "{% if 'web-user-app' not in platforms %}web-user-app{% endif %}"
  - "{% if 'web-admin-portal' not in platforms %}web-admin-portal{% endif %}"
  - "{% if 'backend' not in platforms %}infra{% endif %}"
  - "{% if not use_docker %}docker-compose.yml{% endif %}"
  - "{% if 'web-user-app' not in platforms %}.github/workflows/web-user-app.yml{% endif %}"
  - "{% if 'web-admin-portal' not in platforms %}.github/workflows/web-admin-portal.yml{% endif %}"
  - "{% if 'mobile-android' not in platforms %}.github/workflows/mobile-android.yml{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}.github/workflows/mobile-ios.yml{% endif %}"
  - "{% if 'mobile-android' not in platforms %}.cursor/rules/mobile-android.mdc{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}.cursor/rules/mobile-ios.mdc{% endif %}"
  - "{% if 'web-user-app' not in platforms and 'web-admin-portal' not in platforms %}.cursor/rules/web.mdc{% endif %}"
  - "{% if 'mobile-android' not in platforms %}.agents/skills/android-build-verify{% endif %}"
  - "{% if 'mobile-android' not in platforms %}.agents/skills/android-contract-alignment{% endif %}"
  - "{% if 'mobile-android' not in platforms %}.agents/skills/android-conventions{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}.agents/skills/ios-build-verify{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}.agents/skills/ios-contract-alignment{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}.agents/skills/ios-conventions{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}.agents/skills/ios-feature-delivery{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}.agents/skills/swiftui-design-system{% endif %}"
  - "{% if 'mobile-android' not in platforms %}.claude/skills/android-build-verify{% endif %}"
  - "{% if 'mobile-android' not in platforms %}.claude/skills/android-contract-alignment{% endif %}"
  - "{% if 'mobile-android' not in platforms %}.claude/skills/android-conventions{% endif %}"
  - "{% if 'mobile-android' not in platforms %}.claude/skills/android-feature-delivery{% endif %}"
  - "{% if 'mobile-android' not in platforms %}.claude/skills/compose-design-system{% endif %}"
  - "{% if 'mobile-android' not in platforms and 'mobile-ios' not in platforms %}.claude/skills/deploy-device{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}.claude/skills/ios-build-verify{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}.claude/skills/ios-contract-alignment{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}.claude/skills/ios-conventions{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}.claude/skills/ios-feature-delivery{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}.claude/skills/swiftui-design-system{% endif %}"
  - "{% if 'web-user-app' not in platforms and 'web-admin-portal' not in platforms %}docs/deployment/cloudflare-setup.md{% endif %}"
  - "{% if 'web-user-app' not in platforms and 'web-admin-portal' not in platforms %}_templates/page{% endif %}"
  - "{% if 'mobile-android' not in platforms %}_templates/feature/new/android-screen.kt.ejs.t{% endif %}"
  - "{% if 'mobile-android' not in platforms %}_templates/feature/new/android-viewmodel.kt.ejs.t{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}_templates/feature/new/ios-view.swift.ejs.t{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}_templates/feature/new/ios-viewmodel.swift.ejs.t{% endif %}"
  - "{% if 'mobile-android' not in platforms and 'mobile-ios' not in platforms %}_templates/screen{% endif %}"
  - "{% if 'mobile-android' not in platforms %}_templates/screen/new/android-screen.kt.ejs.t{% endif %}"
  - "{% if 'mobile-android' not in platforms %}_templates/screen/new/android-viewmodel.kt.ejs.t{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}_templates/screen/new/ios-view.swift.ejs.t{% endif %}"
  - "{% if 'mobile-ios' not in platforms %}_templates/screen/new/ios-viewmodel.swift.ejs.t{% endif %}"

# =============================================================================
# Project Identity
# =============================================================================

project_name:
  type: str
  help: "Human-readable project name (e.g., My Awesome App)"
  validator: "{% if not project_name %}Project name is required{% endif %}"

project_slug:
  type: str
  help: "Lowercase slug for directories and packages (e.g., my-awesome-app)"
  default: "{{ project_name | lower | replace(' ', '-') | replace('_', '-') }}"

package_identifier:
  type: str
  help: "Reverse-domain package identifier (e.g., com.example.myapp)"
  default: "com.example.{{ project_slug | replace('-', '') }}"

description:
  type: str
  help: "One-line project description"
  default: "A multi-platform application"

package_path:
  type: str
  help: "Package path derived from package identifier (used for directory names)"
  default: "{{ package_identifier | replace('.', '/') }}"
  when: false

ios_module_name:
  type: str
  help: "Valid Swift/Xcode module name derived from the project slug"
  default: "{{ project_slug | replace('-', ' ') | replace('_', ' ') | title | replace(' ', '') }}"
  when: false

# =============================================================================
# Platform Selection
# =============================================================================

platforms:
  type: yaml
  multiselect: true
  help: "Which platforms do you want to include?"
  choices:
    Spring Boot Backend: backend
    User-Facing Web App: web-user-app
    Admin Web Portal: web-admin-portal
    Android (Kotlin/Compose): mobile-android
    iOS (Swift/SwiftUI): mobile-ios
  default: "[backend, web-user-app, web-admin-portal, mobile-android, mobile-ios]"

# =============================================================================
# Authentication
# =============================================================================

auth_methods:
  type: yaml
  multiselect: true
  help: "Which authentication methods do you want to support? Keep Username + Password selected; OAuth providers are additive."
  validator: "{% if 'password' not in auth_methods %}Prism currently requires Username + Password auth as the baseline sign-in method{% endif %}"
  choices:
    Username + Password (required): password
    Google OAuth: google
    Apple Sign-In: apple
    Facebook Login: facebook
    Microsoft Account: microsoft
  default: "[google, password]"

# =============================================================================
# Infrastructure
# =============================================================================

database:
  type: str
  help: "Which database do you want to use?"
  choices:
    PostgreSQL (Currently available): postgres
  default: postgres

supporting_services:
  type: yaml
  multiselect: true
  help: "Which optional supporting services do you want to include?"
  when: "{{ 'backend' in platforms }}"
  choices:
    Redis cache: redis
  default: "[]"

use_docker:
  type: bool
  help: "Include Docker Compose for local development?"
  default: true

# =============================================================================
# Deployment & Hosting
# =============================================================================

cloud_provider:
  type: str
  help: "Where should backend services be deployed?"
  choices:
    Azure (Currently available): azure
  default: azure

web_hosting:
  type: str
  help: "Where should web applications be deployed?"
  when: "{{ 'web-user-app' in platforms or 'web-admin-portal' in platforms }}"
  choices:
    Cloudflare via OpenNext (Currently available): cloudflare
  default: cloudflare

# =============================================================================
# Repository
# =============================================================================

github_org:
  type: str
  help: "GitHub organization or username (leave empty to skip)"
  default: ""
