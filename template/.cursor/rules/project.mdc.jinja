---
alwaysApply: true
---

# {{ project_name }}

{{ description }}

## Architecture

Monorepo, MVVM on all platforms, API-first (OpenAPI -> typed clients).

{% if "backend" in platforms %}- **Backend**: Spring Boot 4, Kotlin 2.2+, Java 21 - Azure Container Apps{% endif %}
{% if "web" in platforms %}- **Web**: Next.js + TypeScript - Cloudflare Pages{% endif %}
{% if "admin" in platforms %}- **Admin**: Next.js + TypeScript - Cloudflare Pages{% endif %}
{% if "android" in platforms %}- **Android**: Kotlin + Jetpack Compose, Hilt, Retrofit 3, Room{% endif %}
{% if "ios" in platforms %}- **iOS**: Swift 6 + SwiftUI, async/await, Keychain, SwiftData (Mac/Xcode only){% endif %}

## Key Conventions

- API-first: define in `shared/api-contracts/openapi.yml` -> generate clients -> implement
- MVVM: one ViewModel per screen, sealed UiState/ViewState enum
- Repository pattern: interface/protocol + implementation
- Feature docs in `docs/features/` with advisory board review
- Entity docs in `backend/docs/entities/`
- Auth: JWT tokens, OAuth ({% for m in auth_methods %}{{ m }}{% if not loop.last %}, {% endif %}{% endfor %})

## Commands

- `task generate-clients` - regenerate typed API clients
{% if "backend" in platforms %}- `task backend:run` / `task backend:test`{% endif %}
{% if "web" in platforms %}- `task web:dev`{% endif %}
{% if "android" in platforms %}- `task android:build` / `task android:test`{% endif %}
{% if "ios" in platforms %}- `task ios:build` / `task ios:test` (Mac only){% endif %}

## Documentation

- `docs/architecture.md` - system overview
- `docs/features/` - feature specs with board review
- `docs/api/conventions.md` - API patterns
- `docs/advisory-board.md` - virtual advisory board
{% if "backend" in platforms %}- `backend/docs/entities/` - entity definitions
- `backend/docs/azure-setup.md` - Azure deployment
{% endif %}
