import SwiftUI
import SwiftData

@main
struct {{ ios_module_name }}App: App {
    @State private var container = DependencyContainer()

    var body: some Scene {
        WindowGroup {
            AppRouter()
                .environment(container)
        }
        .modelContainer(for: [CachedExample.self])
    }
}
