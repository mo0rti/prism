# Architecture Overview - {{ project_name }}

## System Description

{{ description }}

## Platform Map

| Platform | Tech Stack | Hosting | Purpose |
|----------|-----------|---------|---------|
{% if "backend" in platforms %}| Backend | Spring Boot 4, Kotlin 2.2+, Java 21 | Azure Container Apps | REST API, business logic, auth |
{% endif %}{% if "web-user-app" in platforms %}| User Web App | Next.js, TypeScript, Tailwind CSS, next-intl, NextAuth | Cloudflare via OpenNext | User-facing marketing and authenticated dashboard |
{% endif %}{% if "web-admin-portal" in platforms %}| Admin Web Portal | Next.js, TypeScript, Tailwind CSS, NextAuth | Cloudflare via OpenNext | Internal admin operations and management UI |
{% endif %}{% if "mobile-android" in platforms %}| Android | Kotlin, Jetpack Compose, Hilt | Google Play | Native Android app |
{% endif %}{% if "mobile-ios" in platforms %}| iOS | Swift, SwiftUI | App Store | Native iOS app |
{% endif %}| Database | PostgreSQL 16 | Azure Database | Data persistence |

## Architecture Pattern

{% if "mobile-android" in platforms or "mobile-ios" in platforms %}
### Mobile: MVVM (Model-View-ViewModel)

Both Android and iOS follow the same MVVM architecture:

```
View (UI)  ->  ViewModel (State + Logic)  ->  Repository  ->  Data Source (API/Local)
```

| Layer | Android | iOS |
|-------|---------|-----|
| View | `@Composable` functions | SwiftUI `View` structs |
| ViewModel | `@HiltViewModel` + `StateFlow<UiState>` | `@Observable` + `ViewState` enum |
| Repository | Interface + Impl (Hilt-injected) | Protocol + Impl (container-injected) |
| API Client | Retrofit + Kotlinx Serialization | URLSession + `async/await` |
| Local Storage | DataStore | Keychain |
| DI | Hilt | Protocol-based container |

{% elif "backend" in platforms and platforms | length == 1 %}
This generation is backend-only. The backend is organized into:

- `bootstrap/` for framework wiring such as security, OpenAPI, CORS, and typed properties
- `shared/` for cross-cutting concerns such as error handling, paging DTOs, and auditing
- `modules/auth/` for registration, login, OAuth callback exchange, JWT refresh, and current-user endpoints
- `modules/transactions/` for transaction CRUD, visibility rules, and ownership checks

{% endif %}
### API Contract

Single source of truth: `shared/api-contracts/openapi.yml`

All platform API clients are **generated** from this spec. Never manually edit generated client code. Run `task generate-clients` after any spec change.

### Authentication

Auth methods: {% for method in auth_methods %}{{ method }}{% if not loop.last %}, {% endif %}{% endfor %}

Flow: Client -> OAuth provider (or login form) -> Backend validates -> Issues JWT -> Client stores token -> Attaches to all API requests.

### Design Tokens

Shared visual language: `shared/design-tokens/tokens.json`

Colors, spacing, typography, and border-radius values consumed by all platforms.

## Key Conventions

1. **API contract first** - define endpoints in OpenAPI before implementing
2. **Documentation first** - create feature/entity docs before coding
3. **Feature modules** - backend domains live under `modules/<domain>/`
4. **Consistent naming** - docs, contract, and code should refer to the same feature names (for example, `transactions`)
5. **Never edit generated code** - modify the OpenAPI spec and regenerate
