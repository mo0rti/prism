# Feature: Authentication

## Board Review

- **Product (Sarah)**: Approved - authentication is a core requirement for any user-facing app
- **Architecture (Marcus)**: Approved - OAuth2 + JWT is the industry standard. Stateless JWT keeps backend simple
- **Security (Aisha)**: Approved with conditions - must use PKCE for OAuth on mobile, rotate refresh tokens, store tokens securely (Keychain on iOS, EncryptedSharedPreferences/DataStore on Android)
- **Business (David)**: Approved - social login reduces signup friction, increases conversion
- **Decision**: Go

## Business Logic

### Supported Methods
{% for method in auth_methods %}- {{ method }}
{% endfor %}

### OAuth Flow
1. Client initiates OAuth flow (opens provider's consent screen)
2. Provider redirects back with authorization code
3. Client sends code to backend: `POST /auth/oauth/callback`
4. Backend exchanges code for provider access token
5. Backend fetches user profile from provider
6. Backend creates or updates User entity
7. Backend issues JWT (access token + refresh token)
8. Client stores tokens and attaches access token to all API requests

{% if "password" in auth_methods %}
### Password Flow
1. **Registration**: Client sends email + password -> `POST /auth/register`
2. Backend validates email uniqueness, hashes password with BCrypt
3. Backend creates User entity, issues JWT
4. **Login**: Client sends email + password -> `POST /auth/login`
5. Backend validates credentials, issues JWT
{% endif %}

### Token Lifecycle
- **Access token**: JWT, expires in 24 hours
- **Refresh token**: opaque string, expires in 30 days
- **Refresh**: `POST /auth/refresh` with refresh token -> new access + refresh tokens
- **Logout**: Client deletes stored tokens

### Account Linking
- If a user authenticates with a new provider but the same email exists, the account is linked (same User, new `authProvider`)

## Entities

- [User](../../backend/docs/entities/user.md) - the authenticated user

## API Endpoints

| Method | Path | Auth | Description |
|--------|------|------|-------------|
{% if "google" in auth_methods or "apple" in auth_methods or "facebook" in auth_methods or "microsoft" in auth_methods %}
| POST | /auth/oauth/callback | No | Exchange OAuth code for JWT |
{% endif %}
{% if "password" in auth_methods %}| POST | /auth/register | No | Register with email + password |
| POST | /auth/login | No | Login with email + password |
{% endif %}| POST | /auth/refresh | No | Refresh access token |
| GET | /auth/me | Yes | Get current user profile |

## Platform Implementation

### Backend
- Controller: `backend/src/.../auth/controller/AuthController.kt`
- Service: `backend/src/.../auth/service/AuthService.kt`
- OAuth2: `backend/src/.../auth/service/OAuth2UserService.kt`
- JWT: `backend/src/.../auth/security/JwtTokenProvider.kt`
- Filter: `backend/src/.../auth/security/JwtAuthenticationFilter.kt`
- Config: `backend/src/.../config/SecurityConfig.kt`, `OAuth2Config.kt`

{% if "web-user-app" in platforms %}
### User Web App
- Pages: `web-user-app/app/[locale]/(public)/login/`, `web-user-app/app/[locale]/dashboard/`
- Auth handlers: `web-user-app/auth.ts`, `web-user-app/app/api/auth/[...nextauth]/route.ts`
- API client: `web-user-app/lib/api/client.ts`
{% endif %}

{% if "web-admin-portal" in platforms %}
### Admin Web Portal
- Pages: `web-admin-portal/app/admin/(auth)/login/`, `web-admin-portal/app/admin/(dashboard)/`
- Auth handlers: `web-admin-portal/auth.ts`, `web-admin-portal/app/api/auth/[...nextauth]/route.ts`
- API client: `web-admin-portal/lib/api/client.ts`
{% endif %}

{% if "mobile-android" in platforms %}
### Android
- Screen: `mobile-android/.../ui/auth/LoginScreen.kt`
- ViewModel: `mobile-android/.../ui/auth/LoginViewModel.kt`
- Repository: `mobile-android/.../data/repository/AuthRepository.kt`
- Token storage: `mobile-android/.../data/local/TokenStorage.kt` (DataStore)
{% endif %}

{% if "mobile-ios" in platforms %}
### iOS
- View: `mobile-ios/.../UI/Auth/LoginView.swift`
- ViewModel: `mobile-ios/.../UI/Auth/LoginViewModel.swift`
- Repository: `mobile-ios/.../Data/Repository/AuthRepository.swift`
- Token storage: `mobile-ios/.../Data/Storage/TokenStorage.swift` (Keychain)
{% endif %}

## Implementation Status

- [x] Feature doc created
- [x] Entity docs created
- [x] Board review completed
- [x] API contract defined in OpenAPI
- [x] API clients generated
- [x] Backend implemented
{% if "web-user-app" in platforms %}
- [x] User web app implemented
{% endif %}
{% if "web-admin-portal" in platforms %}
- [x] Admin portal implemented
{% endif %}
{% if "mobile-android" in platforms %}
- [x] Android implemented
{% endif %}
{% if "mobile-ios" in platforms %}
- [x] iOS implemented
{% endif %}
